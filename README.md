# DSA-Project-Library-Management-System-Java-
A Java-based Library Management System developed as a DSA project with SQL Server integration. It supports adding, issuing, returning, searching, and deleting books with full CRUD operations using OOP principles and JDBC for database connectivity.
